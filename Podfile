project 'PokeApi Client - JSON Decoder.xcodeproj'

# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PokeApi Client - JSON Decoder' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PokeApi Client - JSON Decoder
  
  pod 'Alamofire'
  pod 'Moya'

end
